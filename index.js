const divide = (numerator, denominator) => {
    return numerator / denominator;
  };
  const result = divide(2000, 100);
  console.log(result);
  
  const square = x => x * 2;{

  }

  const add = (a,b) =>{
    return a+b;
  }
  